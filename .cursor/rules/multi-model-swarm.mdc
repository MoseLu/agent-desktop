# 多模型 Agent Swarm 协同规则（增强版）

本规则定义了基于 Agent Swarm（智能体蜂群）模式的多模型协作机制，实现任务自动分配和模型智能调度。

**版本**: 2.0.0 
**更新**: 2026-02-25 - 增强任务识别、成本监控、错误处理、可观测性

---

## 可用模型列表

| 模型标识 | 提供商 | 角色 | 特长 |
|---|-----|---|---|
| `qwen3.5-plus` | 通义千问 | 通用助手 | 均衡能力，适合通用任务 |
| `qwen3-max-2026-01-23` | 通义千问 | 推理分析师 | 复杂推理、架构决策、数学计算 |
| `qwen3-coder-next` | 通义千问 | 主力代码专家 | 最新代码能力、前沿特性、数据分析 |
| `qwen3-coder-plus` | 通义千问 | 代码工程师 | 稳定代码能力、重构优化 |
| `MiniMax-M2.5` | MiniMax | 创意专家 | 创意生成、UI 设计建议 |
| `glm-5` | 智谱 AI | 中文专家 | 中文理解、文档撰写 |
| `glm-4.7` | 智谱 AI | 平衡助手 | 综合能力、中等复杂度任务 |
| `kimi-k2.5` | 月之暗面 | 文档专家 | 长文本处理、代码审查 |

---

## 多源 API 配置（蜂群与 MCP）

蜂群中 **MiniMax-M2.5**（创意专家）可配置为独立 API，与统一 API 并列使用：

- **统一 API**：其余 7 个模型使用同一 base URL 与 key（如 DashScope/代理）。
- **MiniMax 独立 API**：在 MCP 服务（mcp-swarm）或调用侧环境变量中配置后，当选用或指定 `MiniMax-M2.5` 时自动走 MiniMax 自己的 endpoint，不占统一 API 配额。

| 用途 | 环境变量 | 说明 |
|---|----|---|
| 统一 API | `SWARM_API_BASE_URL`、`SWARM_API_KEY`、`SWARM_CHAT_PATH` | 通义/智谱/Kimi 等 |
| MiniMax | `SWARM_MINIMAX_API_BASE_URL`、`SWARM_MINIMAX_API_KEY`、`SWARM_MINIMAX_CHAT_PATH` | 仅当模型为 MiniMax-M2.5 时使用；path 建议为 `chat/completions`（base 已含 `/v1`） |

创意类任务（UI 设计、创意、方案设计、用户体验、界面）会**自动首选 MiniMax-M2.5**；若未配置 MiniMax 独立 API，则按降级策略使用 glm-5。

---

## 任务类型识别（增强版）

### 代码相关任务

```
关键词：编写、实现、重构、优化、debug、修复、函数、类、组件、API、代码、typescript、vue、react、python、go、java、javascript、rust、cpp、编程、开发、测试、单元测试、接口、模块、服务、后端、前端、全栈
优先关键词：实现、编写、开发、编程
首选模型：qwen3-coder-next
备选模型：qwen3-coder-plus, qwen3.5-plus
建议参数：temperature=0.3, max_tokens=8192
```

### 架构与推理任务

```
关键词：架构、设计、分析、方案、决策、评估、优化策略、推理、规划、策略、系统性、方法论、最佳实践、技术选型、权衡、对比、优缺点
优先关键词：架构、设计、分析、决策
首选模型：qwen3-max-2026-01-23
备选模型：glm-5, kimi-k2.5
建议参数：temperature=0.3, max_tokens=8192
```

### 数学计算任务

```
关键词：数学、计算、公式、方程、积分、导数、代数、几何、概率、统计、微积分、线性代数、数值、运算
优先关键词：数学、计算、公式、方程
首选模型：qwen3-max-2026-01-23
备选模型：qwen3-coder-next, qwen3.5-plus
建议参数：temperature=0.3, max_tokens=8192
```

### 数据分析任务

```
关键词：数据、图表、可视化、报表、dashboard、分析、统计、趋势、对比、挖掘、清洗、处理、excel、csv、json
优先关键词：数据、图表、可视化、分析
首选模型：qwen3-coder-next
备选模型：qwen3-max-2026-01-23, qwen3.5-plus
建议参数：temperature=0.5, max_tokens=8192
```

### 中文文档任务

```
关键词：文档、说明、注释、README、文档编写、本地化、中文、翻译、解释、教程、指南、手册、api 文档、技术文档
优先关键词：文档、说明、翻译
首选模型：glm-5
备选模型：glm-4.7, kimi-k2.5
建议参数：temperature=0.5, max_tokens=8192
```

### 长文本处理任务

```
关键词：审查、分析报告、总结、长代码、批量处理、阅读、理解、提取、摘要、概括、全文、长篇、pdf、文件、文章
优先关键词：审查、分析、总结、摘要
首选模型：kimi-k2.5
备选模型：qwen3-max-2026-01-23
建议参数：temperature=0.3, max_tokens=8192
```

### 创意与设计任务

```
关键词：UI 设计、创意、创意、方案设计、用户体验、界面、设计、视觉、配色、布局、交互、原型、wireframe、mockup、美学、艺术、风格
优先关键词：UI 设计、创意、设计、界面
首选模型：MiniMax-M2.5
备选模型：glm-5
建议参数：temperature=0.8, max_tokens=4096
```

### 通用任务

```
关键词：默认、一般、简单、聊天、问答
首选模型：qwen3.5-plus
备选模型：glm-4.7
建议参数：temperature=0.5, max_tokens=4096
```

---

## 协作策略

### 简单任务 (单模型)

```yaml
触发条件:
 - 单文件修改
 - 少于 50 行代码
 - 明确单一目标
策略：直接使用首选模型执行
```

### 中等任务 (主模型 + 验证)

```yaml
触发条件:
 - 多文件修改
 - 50-200 行代码
 - 需要一定推理
策略:
 1. 首选模型执行
 2. 备选模型验证
 3. 整合输出
```

### 复杂任务 (多模型并行)

```yaml
触发条件:
 - 架构设计
 - 跨模块重构
 - 大规模代码生成
策略:
 1. 推理模型分析需求
 2. 代码模型并行生成
 3. 文档模型整理输出
 4. 整合验证结果
```

---

## 模型调用格式

### API 请求示例

```typescript
// 基础调用格式
const response = await fetch('/api/chat', {
 method: 'POST',
 headers: { 'Content-Type': 'application/json' },
 body: JSON.stringify({
 model: 'qwen3-coder-next', // 模型标识
 messages: [...],
 temperature: 0.3,
 max_tokens: 8192
 })
});
```

### 模型参数建议

| 模型类型 | temperature | max_tokens | 备注 |
|---|----|---|---|
| 代码生成 | 0.3-0.5 | 4096-8192 | 低温度保证代码准确性 |
| 创意任务 | 0.8-1.0 | 2048-4096 | 高温度增加创意性 |
| 文档生成 | 0.5-0.7 | 4096-8192 | 平衡质量与连贯性 |
| 分析推理 | 0.3-0.5 | 8192+ | 低温度确保逻辑严谨 |
| 数学计算 | 0.2-0.3 | 8192+ | 极低温度确保准确性 |

---

## 降级策略（增强版）

当首选模型不可用时，按以下顺序降级：

```
qwen3-coder-next → qwen3-coder-plus → qwen3.5-plus
qwen3-max-2026-01-23 → glm-5 → kimi-k2.5
glm-5 → glm-4.7 → qwen3.5-plus
kimi-k2.5 → qwen3-max-2026-01-23
MiniMax-M2.5 → glm-5 → qwen3.5-plus
```

### 熔断机制

- 某模型连续失败 ≥5 次 → 临时熔断 60 秒
- 熔断期间请求自动降级到备选模型
- 60 秒后进入"半开"状态，允许一次试探请求
- 成功则恢复正常，失败则继续熔断

### 错误分类处理

| 错误类型 | 处理策略 |
|---|---|
| timeout | 降级到备用模型，重试 2 次 |
| rate_limit | 降级到备用模型，重试 2 次 |
| api_error | 降级到备用模型 |
| network_error | 重试 1 次后降级 |
| auth_error | 直接报错，不降级 |

---

## 成本监控与配额管理

### 默认成本配置（示例）

| 模型 | 每 1k tokens 成本 | 每次请求成本 |
|---|----|---|
| qwen3-coder-next | $0.002 | $0.001 |
| qwen3-coder-plus | $0.0015 | $0.001 |
| qwen3-max-2026-01-23 | $0.004 | $0.002 |
| qwen3.5-plus | $0.001 | $0.0005 |
| glm-5 | $0.0012 | $0.0005 |
| glm-4.7 | $0.0008 | $0.0003 |
| kimi-k2.5 | $0.003 | $0.001 |
| MiniMax-M2.5 | $0.0025 | $0.001 |

### 预算控制

- **默认日预算**: $50.0
- **告警阈值**: 80%（达到时发出警告）
- **暂停阈值**: 95%（达到时暂停非关键请求）

### 配额管理（可选）

可为每个模型设置：
- 每日请求数限制
- QPS（每秒请求数）限制

---

## 结果验证机制

### 三级验证体系

| 级别 | 验证类型 | 适用场景 |
|---|---|---|
| Level 1 | 基础验证（语法/格式） | 所有任务 |
| Level 2 | 交叉验证（多模型对比） | 重要任务 |
| Level 3 | 外部工具验证（代码执行/数学求值） | 代码/数学任务 |

### 验证输出

响应包含验证元数据：
```
[任务类型：code | 使用模型：qwen3-coder-next | 置信度：85%]

验证状态：✓ 通过
验证详情:
 - JSON 格式验证通过
 - JavaScript 语法检查通过
置信度：0.9
```

---

## 可观测性

### 日志记录

所有请求自动记录：
- 任务分析日志
- 模型选择决策
- 请求耗时
- 错误详情
- 降级事件

### 性能指标

实时监控：
- 平均响应时间
- P95/P99 响应时间
- 成功率
- 请求速率（次/分钟）

### 追踪系统

每个请求生成唯一 trace ID：
- 记录完整调用链
- 包含所有降级和重试
- 支持性能分析

---

## 使用指南

### 自动模式

系统根据任务特征自动选择最优模型，无需手动指定。

**输出示例**:
```
[任务类型：code | 使用模型：qwen3-coder-next | 置信度：85% | 匹配关键词：实现，TypeScript | 推理：匹配到 2 个关键词]
```

### 手动指定

在请求中明确指定模型：

```
使用 qwen3-coder-next 编写一个 TypeScript 工具函数...
```

### 批量协作

对于大型任务，可启用蜂群模式：

```
[蜂群模式] 请分析并重构这个模块...
```

系统将自动：
1. 分析任务复杂度
2. 选择合适的模型组合
3. 并行/串行执行
4. 整合输出结果

### 查询统计

```
/swarm_get_stats - 获取成本和使用统计
/swarm_get_metrics - 获取性能指标
/swarm_get_logs - 查询最近日志
/swarm_reset_circuit_breaker - 重置熔断器
```

---

## 注意事项

1. **API 配额管理**: 注意各模型的调用频率限制
2. **上下文长度**: 不同模型上下文窗口不同，注意截断
3. **响应时间**: 复杂任务多模型协作会增加响应时间
4. **成本控制**: 根据任务复杂度合理选择模型层级
5. **多源蜂群**: MiniMax 已并入蜂群；配置 `SWARM_MINIMAX_*` 后，创意任务自动走 MiniMax API，与统一 API 并行使用
6. **熔断保护**: 连续失败会自动触发熔断，保护系统稳定性
7. **验证机制**: 关键任务建议启用 Level 2 或 Level 3 验证

---

## 新增工具（v2.0）

### swarm_chat
- 自动选择模型
- 支持自定义 temperature 和 max_tokens
- 包含成本检查和熔断保护
- 输出置信度和验证结果

### swarm_chat_with_model
- 指定模型调用
- 支持自定义参数
- 自动降级保护

### swarm_analyze_task
- 仅分析任务类型
- 返回推荐模型和备选列表
- 包含建议参数

### swarm_get_stats
- 成本监控统计
- 模型使用分布
- 预算使用率
- 错误统计
- 熔断器状态

### swarm_get_metrics
- 平均响应时间
- P95/P99 响应时间
- 成功率
- 请求速率

### swarm_get_logs
- 查询最近日志
- 支持按级别过滤
- 包含详细错误信息

### swarm_reset_circuit_breaker
- 重置指定模型熔断器
- 或重置所有模型

---

## 升级说明（v2.0）

### 新增功能
✅ 增强任务识别（新增 math、data_analysis 类型） 
✅ 动态参数配置（temperature、max_tokens 自动建议） 
✅ 成本监控与配额管理 
✅ 三级验证机制 
✅ 熔断器与错误降级 
✅ 完整日志和性能监控 

### 改进
✅ 关键词库扩展（从 6 类到 8 类任务） 
✅ 置信度评分系统 
✅ 优先关键词匹配 
✅ 降级策略优化 

### 下一步（TODO）
⏳ 多跳任务链 DSL 
⏳ 工作流引擎 
⏳ 更复杂的交叉验证

---

## 工作流引擎（v2.1 新增）

### 内置工作流

| 工作流名称 | 用途 | 步骤 |
|-----|---|---|
| `code_review` | 代码审查 | 分析 → 提问 → 建议 |
| `data_analysis` | 数据分析 | 理解 → 分析 → 可视化 |
| `content_creation` | 内容创作 | 大纲 → 草稿 → 润色 |
| `bug_fix` | Bug 修复 | 诊断 → 定位 → 修复 |

### 使用示例

```bash
# 使用内置工作流
/swarm_execute_workflow
 workflowName: code_review
 input: "待审查的代码"

# 自定义工作流
/swarm_execute_workflow
 workflowDefinition: |
 name: my_workflow
 steps:
 - id: step1
 model: qwen3-max-2026-01-23
 prompt: "分析：{{input}}"
 - id: step2
 model: qwen3-coder-next
 inputFrom: previous
 prompt: "编码：{{previous.output}}"
 input: "需求描述"
```

### 高级功能

- **变量替换**: `{{variable}}`, `{{previous.output}}`
- **数据转换**: transform 字段支持 TypeScript 代码
- **错误处理**: fallbackOnError 指定失败跳转
- **重试机制**: retryCount 和 timeout 配置
- **验证工具**: swarm_validate_workflow 验证定义

详细文档参考：`mcp-swarm/WORKFLOW_GUIDE.md`
